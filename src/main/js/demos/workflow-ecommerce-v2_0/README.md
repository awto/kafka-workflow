`workflow-ecommerce-v2_0` is the incompatible major version for the ecommerce versioning demo.

It starts fresh, never adopts `1.x` handoffs, and uses a different reminder shape to make the major split visible in tests.
