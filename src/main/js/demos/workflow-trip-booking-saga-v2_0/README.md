# workflow-trip-booking-saga-v2_0

Business workflow logic for the `2.0.x` trip-booking versioning demo.

This major version starts fresh and books hotel, flight, and taxi directly. It
does not reuse `1.x` workflow state.
