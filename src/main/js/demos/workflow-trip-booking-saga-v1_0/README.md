# workflow-trip-booking-saga-v1_0

Business workflow logic for the `1.0.x` trip-booking versioning demo.

This package is intentionally a plain library. The runnable example bundle lives in `workflow-trip-booking-saga-versioned`.
