# workflow-trip-booking-saga-v1_1

Business workflow logic for the `1.1.x` trip-booking versioning demo.

This version adopts handoff state from `1.0.x` and falls back to a taxi when the car booking is unavailable.
